# - name: Declare variable
#   hosts: all
#   vars: 
#     URL: example.com
#   tasks:
#    - name: Print the URL variable
#      ansible.builtin.debug:
#        msg: URL - {{ URL }}
  

#  register is used for the 